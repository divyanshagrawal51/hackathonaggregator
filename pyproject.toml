[project]
name = "hackathons"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Divyansh Agrawal", email = "divyashagrawal309@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
    "pyyaml>=6.0",
]

[project.scripts]
hackathons = "hackathons:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
